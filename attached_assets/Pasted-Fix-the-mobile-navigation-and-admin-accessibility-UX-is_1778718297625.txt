Fix the mobile navigation and admin accessibility UX issues in the GK Shah Family Directory app.

CURRENT PROBLEM:
On mobile devices:

- users cannot access all desktop sidebar actions
- admin login is hidden
- Create Moment is inaccessible
- advanced features are difficult to discover
- mobile experience feels incomplete compared to desktop

GOAL:
Make mobile fully feature-complete while preserving clean UI.

====================================
MOBILE NAVIGATION REDESIGN

Keep the existing bottom navigation for quick access:

- Home
- Members
- Moments
- Tree
- Settings

BUT ALSO add:

- a proper slide-out mobile drawer/sidebar
- accessible from hamburger menu top-left

The mobile drawer must include ALL desktop sidebar items:

- Dashboard
- Members
- Moments
- Family Tree
- Relationships
- Statistics
- Import
- Settings
- Data Health
- Admin Access
- Backup & Restore

====================================
ADMIN ACCESS FIX

Admin login MUST be visible on mobile.

Inside mobile Settings page:
Add:

- “Admin Access” card/button
- opens password modal
- uses existing admin password logic

Once logged in:
show:

- Create Moment floating button
- Import access
- Edit member actions
- Admin tools

====================================
CREATE MOMENT MOBILE UX

When admin is logged in:
show floating action button (FAB):

- bottom-right
- camera or plus icon
- visible on Moments page

Clicking opens:

- /moments/new

If no memories exist:
show:
“Create First Memory” CTA button in empty state.

====================================
MOBILE DRAWER UX

Requirements:

- smooth slide animation
- backdrop blur
- close on outside tap
- close after navigation
- preserve elegant warm aesthetic

Drawer should feel premium and lightweight.

====================================
RESPONSIVE IMPROVEMENTS

Improve mobile spacing:

- search bars
- filter pills
- typography
- cards
- empty states

Prevent:

- horizontal overflow
- clipped text
- inaccessible controls

====================================
ADMIN STATE INDICATOR

When admin mode is active:
show small subtle badge:
“Admin Mode”
inside drawer/settings.

====================================
TYPE SAFETY

Maintain:

- full TypeScript safety
- null guards
- responsive layouts
- ErrorBoundary compatibility

Do not break existing desktop layout.

This should make mobile experience equal to desktop experience while remaining simple and elegant.